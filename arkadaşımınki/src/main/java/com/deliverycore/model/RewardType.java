package com.deliverycore.model;

/**
 * Defines the type of reward given to event winners.
 */
public enum RewardType {
    /**
     * Give items directly to the player's inventory.
     */
    INVENTORY,
    
    /**
     * Execute commands as rewards.
     */
    COMMAND
}
